/*window.addEventListener("load", function() {
  // Elementos a seleccionar con los correspondientes tipos de clase
  const elementos = document.querySelectorAll(
    ".z-box li div, .z-box a, .z-box li a figure"
  );

  // Clases comunes para todos los elementos
  const clasesComunes = ["d-flex", "ai-ce"];

  // Objeto que define las clases específicas por selector
  /*const clasesEspecificas = {
    ".e-off-canvas .z-box li": ["rad-xxxl", "w-100", "z-mb-0", "z-px-1", "z-py-1"],
  };*/

  // Aplicar las clases comunes a todos los elementos
 /* elementos.forEach(elemento => {
    elemento.classList.add(...clasesComunes); // Agregar clases comunes

    // Aplicar clases específicas según el selector
    for (const selector in clasesEspecificas) {
      if (elemento.matches(selector)) {
        elemento.classList.add(...clasesEspecificas[selector]);
      }
    }
  });
});*/













